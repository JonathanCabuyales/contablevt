import { RecibirDataPipe } from './recibir-data.pipe';

describe('RecibirDataPipe', () => {
  it('create an instance', () => {
    const pipe = new RecibirDataPipe();
    expect(pipe).toBeTruthy();
  });
});
